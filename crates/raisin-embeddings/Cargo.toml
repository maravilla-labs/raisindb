[package]
name = "raisin-embeddings"
version = "0.1.0"
edition = "2021"
description = "Vector embeddings support for RaisinDB"
license = "BSL-1.1"

[lib]
path = "src/lib.rs"

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
rmp-serde = { workspace = true }
tracing = "0.1"
raisin-crypto = { path = "../raisin-crypto" }
chrono = { workspace = true }
uuid = { workspace = true, features = ["v4"] }
raisin-error = { path = "../raisin-error" }
async-trait = "0.1"
reqwest = { version = "0.12", features = ["json"] }
tokio = { workspace = true, features = ["sync", "time"] }
raisin-hlc = { version = "0.1.0", path = "../raisin-hlc" }
raisin-ai = { version = "0.1.0", path = "../raisin-ai" }
# Stable digest for the embedding spec hash — std's DefaultHasher is explicitly
# not stable across Rust releases, and this hash is compared against one written
# by an older binary.
ring = "0.17"

[dev-dependencies]
tokio = { workspace = true, features = ["full"] }
