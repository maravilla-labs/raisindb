/** Path of the AI agent we chat with (lives in the `functions` workspace). */
export const AGENT_PATH = '/agents/shift-planner';

/**
 * The plan-enabled coordinator agent behind the Planner tab
 * (execution_mode approve_then_auto, task_creation_enabled).
 */
export const COORDINATOR_PATH = '/agents/shift-coordinator';
