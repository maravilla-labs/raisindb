# RaisinDB WebSocket endpoint (tenant-less; the server resolves the tenant,
# defaulting to "default"). Multi-tenant operators can use
# ws://host:8081/sys/{tenant}/{repository} instead.
VITE_RAISIN_WS_URL=ws://localhost:8081/ws/shiftboard
VITE_RAISIN_REPO=shiftboard
